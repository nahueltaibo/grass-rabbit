# Pinned: build123d ships OpenCascade kernel changes between releases, which
# can quietly alter exported geometry. Bump deliberately, then re-export and
# diff the STEP before trusting it.
build123d==0.11.1

# Live preview in VS Code. Needs the "OCP CAD Viewer" extension too.
ocp_vscode==4.1.0
